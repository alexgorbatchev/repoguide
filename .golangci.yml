version: "2"
linters:
  enable:
    - gocritic
    - misspell
    - unconvert
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
formatters:
  enable:
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/phobologic/repoguide
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
